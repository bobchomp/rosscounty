# Copy this file to .env.local and fill in real values.

# Supabase project — Project Settings > API in the Supabase dashboard.
# These two are all the app currently needs (the anon key is sometimes
# labelled "publishable key" in newer Supabase projects — same thing).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Not used by anything yet — leave blank for now. Only needed later for
# admin operations that must bypass row-level security (e.g. inviting
# users via the Supabase Admin API). Server-only, never expose to the
# browser if you do add it.
SUPABASE_SERVICE_ROLE_KEY=

# Where the "Shop" link should redirect once the Shopify store is live.
NEXT_PUBLIC_SHOP_URL=

# api-football.com free API key (dashboard.api-football.com after signup).
# Server-only — used by the "Sync from API-Football" buttons in
# /admin/fixtures. See README for how to find your league/team IDs.
API_FOOTBALL_KEY=
