import { AdminPlaceholder } from "@/components/admin/admin-placeholder";

export default function AdminSettingsPage() {
  return (
    <AdminPlaceholder
      title="Settings"
      description="Site-wide settings — including the Shop redirect URL (NEXT_PUBLIC_SHOP_URL) and admin user management — will live here."
    />
  );
}
